#include<stdio.h>
int main(){
    int n,m,a,b,sum,sub;
    
    scanf("%d %d",&n,&m);

    sum=n+m;
    sub=n-m;

printf("The sum is %d and the sub is %d",sum,sub);
    scanf("%d %d",&a,&b);

    sum=a+b;
    sub=a-b;

printf("The sum is %d and the sub is %d",sum,sub);

return 0;

}